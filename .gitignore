/target
# Cached toolchain installs (downloaded release bundles)
/.beamtalk-uat
# Beamtalk build artifacts inside fixture projects
**/_build/
**/*.beam
**/*.core
# Erlang crash dumps from local BEAM scenario runs
erl_crash.dump
